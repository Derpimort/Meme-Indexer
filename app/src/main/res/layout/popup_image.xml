<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/popup_image_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:minHeight="256dp"
    android:background="@color/black"
    android:gravity="center"
    android:orientation="vertical">

    <com.github.chrisbanes.photoview.PhotoView
        android:id="@+id/popup_image_meme"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:contentDescription="@string/meme_file_popup" />

    <!--<ImageButton
        android:id="@+id/popup_image_share"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="-25sp"
        android:layout_weight="1"
        android:background="@color/colorPrimary50"
        android:gravity="end"
        android:tint="@color/colorSecondaryLight"
        app:srcCompat="@android:drawable/ic_menu_share"
        android:contentDescription="@string/meme_file_popup"/>-->
</LinearLayout>